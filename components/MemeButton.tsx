import * as React from 'react';

const MemeButton: React.FC = () => {
  return (
    <header className="title-messsage">
      <button className="meme">Upload a MEME</button>
    </header>
  );
};

export default MemeButton;
